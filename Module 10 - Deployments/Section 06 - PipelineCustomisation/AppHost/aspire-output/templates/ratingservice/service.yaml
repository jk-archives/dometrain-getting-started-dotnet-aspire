---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "ratingservice-service"
  labels:
    app.kubernetes.io/name: "apphost"
    app.kubernetes.io/component: "ratingservice"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "apphost"
    app.kubernetes.io/component: "ratingservice"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.ratingservice.port_http | int }}
      targetPort: {{ .Values.parameters.ratingservice.port_http | int }}
